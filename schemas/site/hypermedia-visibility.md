---
name: "Visibility"
summary: "Resource visibility: \"\" (empty) for public, \"Private\" for private."
---

# Visibility

Resource visibility: "" (empty) for public, "Private" for private.


This document describes the **hypermedia-visibility** type — a Hypermedia Network blob schema. Its formal schema is attached (the `schemaDefinition` in this document's metadata), so the app can show it and create values of this type.

## Shape

Kind: `string`. One of: ``, `Private`.
