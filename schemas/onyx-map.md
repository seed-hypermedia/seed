---
name: "Map"
summary: "A map of string keys to values — Map<Any> until constrained."
---

# Map

A map of string keys to values — Map<Any> until constrained.


This document describes the **onyx-map** type — a primitive. Its formal schema is attached (the `schemaDefinition` in this document's metadata), so the app can show it and create values of this type.

## Shape

Kind: `map`.
