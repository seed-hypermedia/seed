---
name: Grants
summary: "per-agent permissions: the callable set (search / web search / execute), **publish** (signed public writing), and the enabled **MCP servers**."
---
**grants** — per-agent permissions: the callable set (search / web search / execute), **publish** (signed public writing), and the enabled **MCP servers**. Verbs are never grants; they're always on. <!-- id:LJVrqOOs -->
