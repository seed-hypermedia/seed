---
name: Attachment
summary: a child spawned while a step is running belongs to that step; the step row _is_ the way into the child.
---
**attachment** — a child spawned while a step is running belongs to that step; the step row _is_ the way into the child. Joined by **planStepId** (the stable id), label as legacy fallback. <!-- id:Otwq7Wqa -->
