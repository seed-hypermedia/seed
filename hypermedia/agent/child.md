---
name: Child
summary: A child is a run spawned by the delegate verb, either a model child with its own session or a script child in the QuickJS engine.
---
**child** — a run spawned by delegate; a model child gets its own session (thread), a script child runs in the QuickJS engine. <!-- id:QGIGEjGR -->
