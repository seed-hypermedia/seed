---
name: Child
summary: a run spawned by delegate; a model child gets its own session (thread), a script child runs in the QuickJS engine.
---
**child** — a run spawned by delegate; a model child gets its own session (thread), a script child runs in the QuickJS engine. <!-- id:QGIGEjGR -->
