---
name: Delegate
summary: "spawn a child run: a **model child** (give it a **brief**) or a **script child** (give it a `script`)."
---
**delegate** — spawn a child run: a **model child** (give it a **brief**) or a **script child** (give it a `script`). `await: false` = detached. <!-- id:heu3gUxG -->
