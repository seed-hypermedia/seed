---
name: Space Index
summary: "the compact `<space>` block in every system prompt: one line per tool, memory top level, active triggers."
---
**Space index** — the compact `<space>` block in every system prompt: one line per tool, memory top level, active triggers. The agent always knows what it _could_ expand. <!-- id:JSgsrBTV -->
