---
name: Space
summary: "An agent's Space is everything it has, as one addressable tree of memory files, tools, triggers, and its own definition."
---
**Space**: everything an agent _has_, as one addressable tree: `~/memory/` (its files), `~/tools/` (its tools), `~/triggers/` (its automations), and `~/self` (its own definition, read-only). Configuration is content. <!-- id:EioLizGD -->
