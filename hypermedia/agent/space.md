---
name: Space
summary: "everything an agent _has_, as one addressable tree: `~/memory/` (its files), `~/tools/` (its tools), `~/triggers/` (its automations), `~/self` (its own definition, read-only), and later `~/plans/`."
---
**Space** — everything an agent _has_, as one addressable tree: `~/memory/` (its files), `~/tools/` (its tools), `~/triggers/` (its automations), `~/self` (its own definition, read-only), and later `~/plans/`. Configuration is content. <!-- id:EioLizGD -->
