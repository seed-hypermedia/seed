---
name: Runs
summary: "Runs are everything that executes: every turn, child, and script is a durable row in one tree that doubles as the dispatch queue."
---
**Runs** — everything that _executes_. Every turn, child, and script is a run row in a tree; the table doubles as the dispatch queue. Waiting runs hold no resources ("waiting is free"). <!-- id:Dnidzb7V -->
