---
name: Typed result / return_result
summary: a child spawned with an `output` schema must deliver its result through the `return_result` tool; validation errors bounce back for self-correction.
---
**typed result / return_result** — a child spawned with an `output` schema must deliver its result through the `return_result` tool; validation errors bounce back for self-correction._ <!-- id:M9d2fPXp -->
