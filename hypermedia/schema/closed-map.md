---
name: Closed Map
summary: "a `map` schema with `properties` and no `values`: keys outside `properties` are rejected."
---
**Closed map**: a `map` schema with `properties` and no `values`: keys outside `properties` are rejected. The default for structs; what lets the meta-schema reject malformed schemas. Add `values` to make a map open. <!-- id:6hKvuX6y -->
