---
name: Map
summary: A map of string keys to values of one type — Map<Any> until constrained by values. For known, named fields use a struct.
schemaDefinition: ipfs://bafyreico2fnohexzyhl2owvisejuef475t62u3syddgmglsr7hhnh3eglq
---
This document describes the **schema/map** type — a core type: a map whose keys are data, each value matching the `values` schema. When the keys are known field names, the type is a [struct](./struct.md) instead. Its formal schema is attached (the `schemaDefinition` in this document's metadata), so the app can show it and create values of this type. <!-- id:kVoJpXNY -->

# Shape <!-- id:crlkBvsQ -->

Kind: `map`. <!-- id:-28QE2Pc -->
