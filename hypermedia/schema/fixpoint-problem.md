---
name: Fixpoint problem
summary: the impossibility of baking a block's own CID into its own content (a hash preimage), which also means a _cycle_ of CIDs has no encoding order.
---
**Fixpoint problem** — the impossibility of baking a block's own CID into its own content (a hash preimage), which also means a _cycle_ of CIDs has no encoding order. The reason references are **names**, not hashes. ([references](./references.md)) <!-- id:rCmFoFNz -->
