---
name: Self-description
summary: "the property that the meta-schema is a valid instance of itself: `schema/meta-schema` matches its own `union` variant, whose `anyOf` items match its `include` variant, whose targets match the other…"
---
**Self-description** — the property that the meta-schema is a valid instance of itself: `schema/meta-schema` matches its own `union` variant, whose `anyOf` items match its `include` variant, whose targets match the other variants. ([the schema language](./schema-language.md)) <!-- id:Ext9Hcb0 -->
