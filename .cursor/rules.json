{
  "commands": {
    "web:test": {
      "description": "Run web tests",
      "command": "yarn web:test run ${testFile}",
      "variables": {
        "testFile": {
          "type": "string",
          "description": "Name of the test file to run"
        }
      }
    }
  }
}
