; @xstate/react@4 declares react<=18 peers, but this app runs react 19 with Metro
; forcing a single copy (see metro.config.js SINGLETON_PACKAGES). Without this,
; npm install fails with ERESOLVE — locally and on EAS Build.
legacy-peer-deps=true
