// The parser lives in @shm/shared so the desktop can resolve these URLs too; re-exported here for
// the web page and its round-trip test.
export {agentsRouteFromUrl} from '@shm/shared/utils/agents-routing'
