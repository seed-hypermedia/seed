/**
 * Pre-rename alias for `/hm/api/space-image`. Kept so a browser still running
 * the previous bundle can finish an upload during a rolling deploy.
 */
export {action} from './hm.api.space-image'
