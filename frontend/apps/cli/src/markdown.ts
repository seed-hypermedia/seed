/**
 * CLI markdown formatter.
 *
 * The implementation is shared with desktop/assistant code so HM URLs,
 * documents, and comments render consistently across surfaces.
 */

export {blocksToMarkdown, documentToMarkdown} from '@shm/shared/hm-markdown'
export type {MarkdownOptions} from '@shm/shared/hm-markdown'
