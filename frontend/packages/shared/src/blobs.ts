/**
 * Re-export of the blob signing/verification primitives, which now live in
 * `@seed-hypermedia/client` so third-party spaces can consume them from a
 * published package.
 */

export * from '@seed-hypermedia/client/blobs'
